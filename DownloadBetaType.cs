namespace Ketarin
{
    /// <summary>
    /// Determines how to handle beta versions of applications.
    /// </summary>
    public enum DownloadBetaType
    {
        Default = 0,
        Avoid = 1,
        AlwaysDownload = 2
    }
}